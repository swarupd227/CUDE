{
  "name": "cude-platform",
  "version": "3.0.0",
  "description": "CUDE — Configurable Universal Discovery Engine (Agentic Data Discovery Platform)",
  "private": true,
  "scripts": {
    "start": "node start.js",
    "build": "node build.js",
    "dev:backend": "cd backend && node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
